version: '5.0.2'

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - mongodb

  # mongo container
  mongodb:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - my-blog-db:/data/db
volumes:
  my-blog-db:
